import type { RuleProps } from '@react-querybuilder/ts';
export declare const Rule: {
    ({ id, path, rule, translations, schema, actions, disabled: disabledProp, parentDisabled, context, field: fieldProp, operator: operatorProp, value: valueProp, valueSource: valueSourceProp, dragMonitorId, dropMonitorId, dndRef, dragRef, isDragging, isOver, }: RuleProps): JSX.Element;
    displayName: string;
};
