import type { RuleGroupProps } from '@react-querybuilder/ts';
export declare const RuleGroup: {
    ({ id, path, ruleGroup, translations, schema, actions, disabled: disabledProp, parentDisabled, context, combinator: combinatorProp, rules: rulesProp, not: notProp, dragMonitorId, dropMonitorId, previewRef, dragRef, dropRef, isDragging, isOver, }: RuleGroupProps): JSX.Element;
    displayName: string;
};
