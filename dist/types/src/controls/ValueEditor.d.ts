import type { ValueEditorProps } from '@react-querybuilder/ts';
export declare const ValueEditor: {
    ({ operator, value, handleOnChange, title, className, type, inputType, values, listsAsArrays, fieldData, disabled, testID, ...props }: ValueEditorProps): JSX.Element | null;
    displayName: string;
};
